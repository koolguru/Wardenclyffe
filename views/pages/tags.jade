extends ../bootstrap.jade

block body
    .container
        .page-header
            h1 Tags: #{tag}
        each r in results
            .row(style="padding-left:30px;")
                .col-sm-8
                    h4.result-title #{r.title}
                    .description #{r.description}
            hr